class Solution {
public:
    void reverseString(vector<char>& s) {
        int z=s.size();
    for(int i=0;i<s.size()/2;i++)
    {
        
        swap(s[i],s[z-1]);
        z--;
    }
        
    }
};
